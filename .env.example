# Marker API Configuration

# Device for ML models
# Options: cuda (GPU) or cpu
TORCH_DEVICE=cuda

# NVIDIA GPU access (for Docker)
NVIDIA_VISIBLE_DEVICES=all

# Which GPU to use (0, 1, or all)
CUDA_VISIBLE_DEVICES=0

# Server settings
HOST=0.0.0.0
PORT=8001

# Marker settings (optional)
# MARKER_BATCH_SIZE=4          # Batch size for processing (higher = more VRAM)
# MARKER_MAX_PAGES=100         # Max pages per PDF
